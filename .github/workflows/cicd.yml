name: CICD
on:
  push:
jobs:
  release:
    if: github.ref == 'refs/heads/main'
    needs: [ci]
    uses: LucasCarioca/workflows/.github/workflows/release-docker.yml@v0.9.9
    with:
      registry: ghcr.io
      image_name: lucascarioca/breath
    secrets:
      username: ${{ github.actor }}
      password: ${{ secrets.GITHUB_TOKEN }}
